openai>=1.30.0
streamlit>=1.30.0
pypdf>=4.0.0
python-docx>=1.1.0
python-dotenv>=1.0.0

# OCR for scanned PDFs. Also needs the Tesseract program itself. packages.txt
# installs it on Streamlit Community Cloud; README.md covers local setup.
pymupdf>=1.24.0
pytesseract>=0.3.10
pillow>=10.0.0

# Typesetting the emailed review. ReportLab ships as a wheel with no system
# libraries behind it, which is why it is here instead of WeasyPrint: an
# HTML-to-PDF engine would need pango and cairo through packages.txt, and a
# deploy that dies on a missing shared object is worse than writing the layout.
reportlab>=4.0.0

# Orders in Postgres. Required on any host with an ephemeral filesystem, which
# includes Streamlit Community Cloud, where the SQLite fallback silently loses
# every order on restart. Point DATABASE_URL at Supabase's transaction pooler.
psycopg[binary]>=3.1
