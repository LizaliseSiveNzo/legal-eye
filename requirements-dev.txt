# Test-only dependencies, kept out of requirements.txt so they are not
# installed on every cold start of the deployed app.
#   pip install -r requirements-dev.txt
-r requirements.txt

pytest>=8.0.0
reportlab>=4.0.0
